def get_last_index(inventory):
    length  = len(inventory)
    print(length)
    return length

def main():
    inventory = ["Shield", "Sword", "Bow", "Arrows", "Health Potion", 5]
    get_last_index(inventory)
main()